# Usage Instructions #

Once this library is installed, it can be called from CLI:

```bash
bloatectomy_ectomy
```

Invoke the help flag on any available argument to learn more:

```
bloatectomy_ectomy remove_bloatectomy --help
```